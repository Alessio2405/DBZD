experiment_revision: phase0_final_r3
model_name: tiny
fallback_model: null
tokenizer_name: simple
data_dir: data/smoke
run_root: runs/smoke
fork_layers: 2
num_zones: 7
max_length: 160
alpha_init: 0.3
lambda_zone: 0.5
gamma_reg: 0.001
epochs: 1
batch_size: 4
eval_batch_size: 4
grad_accum_steps: 1
learning_rate: 0.001
weight_decay: 0.01
warmup_ratio: 0.1
max_grad_norm: 1.0
max_steps: 3
eval_every_steps: 2
save_every_steps: 2
log_every_steps: 1
max_eval_batches: 1
answer_eval_examples: 1
answer_batch_size: 1
answer_max_new_tokens: 8
answer_length_percentile: 0.99
answer_length_margin_tokens: 20
generation_sample_count: 1
gradient_cosine_params: 2
precision: fp32
num_workers: 0
compact_completed_checkpoint: false
